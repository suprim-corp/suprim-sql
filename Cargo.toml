[workspace]
members = ["crates/suprim-core", "crates/suprim-app"]
resolver = "2"

# ---------------------------------------------------------
# Build profiles (shared across workspace)
# ---------------------------------------------------------
[profile.dev]
opt-level = 0

[profile.release]
opt-level     = 3
lto           = true
codegen-units = 1
strip         = true
